principium
numerus a, b.
textus nome.
scribe("Digite seu nome: ").
lege(nome).
a = 10.
b = 5 + 2 * 3.
si (b > a) {
    scribe(nome).
    scribe(" Venceu!").
} aliter {
    dum (a > 0) {
        a = a - 1.
    }
}
pro (contador = 0 usque 5) {
    scribe(contador).
}
finis